source 'https://rubygems.org'

# ruby '2.1.0'

gem 'guard'
gem 'listen'
gem 'guard-rspec'
gem 'terminal-notifier'
gem 'terminal-notifier-guard'

# Because when I ran `listen` in debug mode it gave this message:
# TCP forwarding requires Celluloid::IO to be present. Please install or add as a dependency: gem 'celluloid-io' (RuntimeError)
gem 'celluloid-io'

gem 'rspec'
